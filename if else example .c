#include<stdio.h>
main()
{
	int a;
	printf("enter the value");
	scanf("%d",&a);
	if (a==0)
	{
	printf("user entered value is zero");
    }
    else
    {
    printf("user entered value is not zero");
	}
}
