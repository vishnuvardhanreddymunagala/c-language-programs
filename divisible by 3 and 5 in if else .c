#include<stdio.h>
main()
{
	int a;
	printf("enter the value");
	scanf("%d",&a);
	if (a%3==0)
	{
		if(a%5==0)
	{
	printf("a is divisible by 3 and 5",a);
   }
	}
	else
	{
	printf("a is not divisible by 3 and 5",a);
	}
}
